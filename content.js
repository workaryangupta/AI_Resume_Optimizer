// function extractJobDetails() {
//     let jobTitle = document.querySelector('h1')?.innerText || 'Not found';
//     let jobDescription = document.querySelector("p")?.innerText || 'Not found';
    
//     chrome.runtime.sendMessage({ action: "jobDetailsExtracted", data: { title: jobTitle, description: jobDescription } });
// }